Faça o cálculo do tempo de execução dos  algoritmos que constam  no anexo.

para a contagem Considere os tempos de cada instrução:
Atribuição
lógica (comparação)
aritmética
Acesso ao array

Deverá ser criado um arquivo para cada resposta (São 3)
identifique no arquivo o algoritmo
Numere cada linha do algoritmo
Para cada linha, informa a quantidade de instruções (L1; l2; Ln)
Elaborar a função algébrica 
Para responder a esta tarefa, gravar um arquivo, utilizando a sintaxe   Markdown.
Para cada busca :
nas buscas BuscaLinear(A,n,x) e BuscaLinearEmordem9A,n,x), Considerar que x está em A[1..n]
Tempo total
Primeira posição
ultima posição
Na  BuscalBinaria(A,n,x) mostre o tempo total 
